<UserControl x:Class="SHC_Rebalancer.ResourcesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SHC_Rebalancer"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance local:ResourcesContext}">

    <DockPanel>
        <!-- configs -->
        <se:StswLabel DockPanel.Dock="Top" Content="Selected config:" FontWeight="Bold" Margin="3 6 3 0"/>
        <local:ConfigBox DockPanel.Dock="Top"
                         ItemsSource="{Binding Configs_resources}"
                         SelectedItem="{Binding SelectedConfig, Mode=OneWayToSource}"
                         SelectedValue="{Binding ConfigName_resources, Mode=OneWay, Source={x:Static local:Settings.Default}}"/>
        <!-- selector -->
        <se:StswSegment ItemsSource="{Binding SelectedConfig.Values}" Margin="3" Orientation="Vertical" SelectedItem="{Binding SelectedPair}">
            <se:StswSegment.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Image Source="{Binding Key, Converter={local:EnumToImageSourceConverter}}" Height="24" Width="24"/>
                        <se:StswText FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" Text="{Binding Key}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </se:StswSegment.ItemTemplate>
        </se:StswSegment>
        <!-- data -->
        <se:StswScrollView IsEnabled="{Binding SelectedPair, Converter={x:Static se:StswNotNullConverter.Instance}}" Margin="3">
            <se:StswLabelPanel DataContext="{Binding SelectedPair.Value}" LabelFontWeight="Medium" LabelHorizontalAlignment="Right" VerticalAlignment="Center" Width="300">

                <se:StswLabel Content="Buy:" IconSource="/Images/resources/Gold.png"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding Buy}"/>

                <se:StswLabel Content="Sell:" IconSource="/Images/resources/Gold.png"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding Sell}"/>

            </se:StswLabelPanel>
        </se:StswScrollView>
    </DockPanel>
</UserControl>
