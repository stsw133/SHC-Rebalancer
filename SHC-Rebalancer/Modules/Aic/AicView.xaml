<UserControl x:Class="SHC_Rebalancer.AicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SHC_Rebalancer"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance local:AicContext}">

    <DockPanel>
        <!-- configs -->
        <se:StswLabel DockPanel.Dock="Top" Content="Selected config:" FontWeight="Bold" Margin="3 6 3 0"/>
        <local:ConfigBox DockPanel.Dock="Top"
                         ItemsSource="{Binding Configs_aic}"
                         SelectedItem="{Binding SelectedConfig, Mode=OneWayToSource}"
                         SelectedValue="{Binding ConfigName_aic, Mode=OneWay, Source={x:Static local:Settings.Default}}"/>
        <!-- selector -->
        <se:StswSegment ItemsSource="{Binding SelectedConfig.Values}" Margin="3" Orientation="Vertical" SelectedItem="{Binding SelectedPair}">
            <se:StswSegment.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Image Source="{Binding Key, Converter={local:EnumToImageSourceConverter}}" Height="32" Width="32"/>
                        <se:StswText FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" Text="{Binding Key}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </se:StswSegment.ItemTemplate>
        </se:StswSegment>
        <!-- data -->
        <se:StswScrollView IsEnabled="{Binding SelectedPair, Converter={x:Static se:StswNotNullConverter.Instance}}" Margin="3">
            <se:StswLabelPanel DataContext="{Binding SelectedPair.Value}" LabelFontWeight="Medium" LabelHorizontalAlignment="Right" VerticalAlignment="Center" Width="400">

                <se:StswLabel Content="WallDecoration:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding WallDecoration}"/>

                <se:StswLabel Content="Unknown001:"/>
                <se:StswCheckBox Margin="3" HorizontalAlignment="Left" IsChecked="{Binding Unknown001}"/>
                
                <se:StswLabel Content="Unknown002:"/>
                <se:StswCheckBox Margin="3" HorizontalAlignment="Left" IsChecked="{Binding Unknown002}"/>
                
                <se:StswLabel Content="Unknown003:"/>
                <se:StswCheckBox Margin="3" HorizontalAlignment="Left" IsChecked="{Binding Unknown003}"/>
                
                <se:StswLabel Content="Unknown004:"/>
                <se:StswCheckBox Margin="3" HorizontalAlignment="Left" IsChecked="{Binding Unknown004}"/>
                
                <se:StswLabel Content="Unknown005:"/>
                <se:StswCheckBox Margin="3" HorizontalAlignment="Left" IsChecked="{Binding Unknown005}"/>

                <se:StswLabel Content="CriticalPopularity:"/>
                <StackPanel Margin="3">
                    <se:StswText FontSize="10" HorizontalAlignment="Center" Text="{Binding CriticalPopularity, Mode=OneWay, Converter={x:Static se:StswCalculateConverter.Instance}, ConverterParameter=/100}"/>
                    <se:StswSlider Minimum="0" Maximum="10000" IsSelectionRangeEnabled="True" IsSnapToTickEnabled="True" SelectionEnd="{Binding Value, RelativeSource={RelativeSource Self}}" TickFrequency="100" Value="{Binding CriticalPopularity}"/>
                </StackPanel>

                <se:StswLabel Content="LowestPopularity:"/>
                <StackPanel Margin="3">
                    <se:StswText FontSize="10" HorizontalAlignment="Center" Text="{Binding LowestPopularity, Mode=OneWay, Converter={x:Static se:StswCalculateConverter.Instance}, ConverterParameter=/100}"/>
                    <se:StswSlider Minimum="0" Maximum="10000" IsSelectionRangeEnabled="True" IsSnapToTickEnabled="True" SelectionEnd="{Binding Value, RelativeSource={RelativeSource Self}}" TickFrequency="100" Value="{Binding LowestPopularity}"/>
                </StackPanel>

                <se:StswLabel Content="HighestPopularity:"/>
                <StackPanel Margin="3">
                    <se:StswText FontSize="10" HorizontalAlignment="Center" Text="{Binding HighestPopularity, Mode=OneWay, Converter={x:Static se:StswCalculateConverter.Instance}, ConverterParameter=/100}"/>
                    <se:StswSlider Minimum="0" Maximum="10000" IsSelectionRangeEnabled="True" IsSnapToTickEnabled="True" SelectionEnd="{Binding Value, RelativeSource={RelativeSource Self}}" TickFrequency="100" Value="{Binding HighestPopularity}"/>
                </StackPanel>
                
                <se:StswLabel Content="TaxesMin:"/>
                <StackPanel Margin="3">
                    <se:StswText FontSize="10" HorizontalAlignment="Center" Text="{Binding TaxesMin, Mode=OneWay}"/>
                    <se:StswSlider Minimum="0" Maximum="11" IsSelectionRangeEnabled="True" IsSnapToTickEnabled="True" SelectionEnd="{Binding Value, RelativeSource={RelativeSource Self}}" TickFrequency="1" Value="{Binding TaxesMin}"/>
                </StackPanel>

                <se:StswLabel Content="TaxesMax:"/>
                <StackPanel Margin="3">
                    <se:StswText FontSize="10" HorizontalAlignment="Center" Text="{Binding TaxesMax, Mode=OneWay}"/>
                    <se:StswSlider Minimum="0" Maximum="11" IsSelectionRangeEnabled="True" IsSnapToTickEnabled="True" SelectionEnd="{Binding Value, RelativeSource={RelativeSource Self}}" TickFrequency="1" Value="{Binding TaxesMax}"/>
                </StackPanel>

                <se:StswLabel Content="Unknown011:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Value="{Binding Unknown011}"/>

                <se:StswLabel Content="Farm1:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Building}" SelectedValue="{Binding Farm1}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="Farm2:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Building}" SelectedValue="{Binding Farm2}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="Farm3:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Building}" SelectedValue="{Binding Farm3}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="Farm4:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Building}" SelectedValue="{Binding Farm4}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="Farm5:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Building}" SelectedValue="{Binding Farm5}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="Farm6:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Building}" SelectedValue="{Binding Farm6}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="Farm7:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Building}" SelectedValue="{Binding Farm7}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="Farm8:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Building}" SelectedValue="{Binding Farm8}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="PopulationPerFarm:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding PopulationPerFarm}"/>

                <se:StswLabel Content="PopulationPerWoodcutter:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding PopulationPerWoodcutter}"/>

                <se:StswLabel Content="PopulationPerQuarry:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding PopulationPerQuarry}"/>

                <se:StswLabel Content="PopulationPerIronmine:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding PopulationPerIronmine}"/>

                <se:StswLabel Content="PopulationPerPitchrig:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding PopulationPerPitchrig}"/>

                <se:StswLabel Content="MaxQuarries:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding MaxQuarries}"/>

                <se:StswLabel Content="MaxIronmines:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding MaxIronmines}"/>

                <se:StswLabel Content="MaxWoodcutters:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding MaxWoodcutters}"/>

                <se:StswLabel Content="MaxPitchrigs:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding MaxPitchrigs}"/>

                <se:StswLabel Content="MaxFarms:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding MaxFarms}"/>

                <se:StswLabel Content="BuildInterval:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding BuildInterval}"/>

                <se:StswLabel Content="ResourceRebuildDelay:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding ResourceRebuildDelay}"/>

                <se:StswLabel Content="MaxFood:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MaxFood}"/>

                <se:StswLabel Content="MinimumApples:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MinimumApples}"/>

                <se:StswLabel Content="MinimumCheese:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MinimumCheese}"/>

                <se:StswLabel Content="MinimumBread:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MinimumBread}"/>

                <se:StswLabel Content="MinimumWheat:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MinimumWheat}"/>

                <se:StswLabel Content="MinimumHop:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MinimumHop}"/>

                <se:StswLabel Content="TradeAmountFood:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding TradeAmountFood}"/>

                <se:StswLabel Content="TradeAmountEquipment:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding TradeAmountEquipment}"/>

                <se:StswLabel Content="AIRequestDelay:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AIRequestDelay}"/>

                <se:StswLabel Content="MinimumGoodsRequiredAfterTrade:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding MinimumGoodsRequiredAfterTrade}"/>

                <se:StswLabel Content="DoubleRationsFoodThreshold:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding DoubleRationsFoodThreshold}"/>

                <se:StswLabel Content="MaxWood:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MaxWood}"/>

                <se:StswLabel Content="MaxStone:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MaxStone}"/>

                <se:StswLabel Content="MaxResourceOther:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MaxResourceOther}"/>

                <se:StswLabel Content="MaxEquipment:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MaxEquipment}"/>

                <se:StswLabel Content="MaxBeer:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="-1" Value="{Binding MaxBeer}"/>

                <se:StswLabel Content="MaxResourceVariance:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding MaxResourceVariance}"/>

                <se:StswLabel Content="RecruitGoldThreshold:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding RecruitGoldThreshold}"/>

                <se:StswLabel Content="BlacksmithSetting:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding BlacksmithSetting}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="FletcherSetting:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding FletcherSetting}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="PoleturnerSetting:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding PoleturnerSetting}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource01:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource01}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource02:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource02}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource03:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource03}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource04:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource04}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource05:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource05}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource06:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource06}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource07:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource07}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource08:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource08}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource09:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource09}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource10:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource10}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource11:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource11}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource12:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource12}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource13:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource13}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource14:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource14}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SellResource15:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Resource}" SelectedValue="{Binding SellResource15}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefWallPatrolRallyTime:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding DefWallPatrolRallyTime}"/>

                <se:StswLabel Content="DefWallPatrolGroups:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding DefWallPatrolGroups}"/>

                <se:StswLabel Content="DefSiegeEngineGoldThreshold:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding DefSiegeEngineGoldThreshold}"/>

                <se:StswLabel Content="DefSiegeEngineBuildDelay:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding DefSiegeEngineBuildDelay}"/>

                <se:StswLabel Content="Unknown072:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Value="{Binding Unknown072}"/>

                <se:StswLabel Content="Unknown073:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Value="{Binding Unknown073}"/>

                <se:StswLabel Content="RecruitProbDefDefault:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbDefDefault}"/>

                <se:StswLabel Content="RecruitProbDefWeak:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbDefWeak}"/>

                <se:StswLabel Content="RecruitProbDefStrong:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbDefStrong}"/>

                <se:StswLabel Content="RecruitProbRaidDefault:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbRaidDefault}"/>

                <se:StswLabel Content="RecruitProbRaidWeak:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbRaidWeak}"/>

                <se:StswLabel Content="RecruitProbRaidStrong:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbRaidStrong}"/>

                <se:StswLabel Content="RecruitProbAttackDefault:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbAttackDefault}"/>

                <se:StswLabel Content="RecruitProbAttackWeak:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbAttackWeak}"/>

                <se:StswLabel Content="RecruitProbAttackStrong:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding RecruitProbAttackStrong}"/>

                <se:StswLabel Content="SortieUnitRangedMin:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Maximum="100" Value="{Binding SortieUnitRangedMin}"/>

                <se:StswLabel Content="SortieUnitRanged:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SortieUnitRanged}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SortieUnitMeleeMin:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding SortieUnitMeleeMin}"/>

                <se:StswLabel Content="SortieUnitMelee:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SortieUnitMelee}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefDiggingUnitMax:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding DefDiggingUnitMax}"/>

                <se:StswLabel Content="DefDiggingUnit:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefDiggingUnit}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RecruitInterval:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding RecruitInterval}"/>

                <se:StswLabel Content="RecruitIntervalWeak:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding RecruitIntervalWeak}"/>

                <se:StswLabel Content="RecruitIntervalStrong:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding RecruitIntervalStrong}"/>

                <se:StswLabel Content="DefTotal:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding DefTotal}"/>

                <se:StswLabel Content="OuterPatrolGroupsCount:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding OuterPatrolGroupsCount}"/>

                <se:StswLabel Content="OuterPatrolGroupsMove:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding OuterPatrolGroupsMove}"/>

                <se:StswLabel Content="OuterPatrolRallyDelay:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding OuterPatrolRallyDelay}"/>

                <se:StswLabel Content="DefWalls:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding DefWalls}"/>

                <se:StswLabel Content="DefUnit1:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefUnit1}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefUnit2:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefUnit2}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefUnit3:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefUnit3}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefUnit4:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefUnit4}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefUnit5:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefUnit5}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefUnit6:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefUnit6}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefUnit7:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefUnit7}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="DefUnit8:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding DefUnit8}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RaidUnitsBase:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding RaidUnitsBase}"/>

                <se:StswLabel Content="RaidUnitsRandom:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding RaidUnitsRandom}"/>

                <se:StswLabel Content="RaidUnit1:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding RaidUnit1}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RaidUnit2:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding RaidUnit2}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RaidUnit3:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding RaidUnit3}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RaidUnit4:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding RaidUnit4}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RaidUnit5:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding RaidUnit5}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RaidUnit6:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding RaidUnit6}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RaidUnit7:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding RaidUnit7}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="RaidUnit8:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding RaidUnit8}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEngine1:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:HarassingUnit}" SelectedValue="{Binding HarassingSiegeEngine1}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEngine2:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:HarassingUnit}" SelectedValue="{Binding HarassingSiegeEngine2}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEngine3:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:HarassingUnit}" SelectedValue="{Binding HarassingSiegeEngine3}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEngine4:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:HarassingUnit}" SelectedValue="{Binding HarassingSiegeEngine4}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEngine5:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:HarassingUnit}" SelectedValue="{Binding HarassingSiegeEngine5}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEngine6:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:HarassingUnit}" SelectedValue="{Binding HarassingSiegeEngine6}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEngine7:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:HarassingUnit}" SelectedValue="{Binding HarassingSiegeEngine7}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEngine8:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:HarassingUnit}" SelectedValue="{Binding HarassingSiegeEngine8}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="HarassingSiegeEnginesMax:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding HarassingSiegeEnginesMax}"/>

                <se:StswLabel Content="RaidRetargetDelay:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding RaidRetargetDelay}"/>

                <se:StswLabel Content="AttForceBase:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttForceBase}"/>

                <se:StswLabel Content="AttForceRandom:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttForceRandom}"/>

                <se:StswLabel Content="AttForceSupportAllyThreshold:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttForceSupportAllyThreshold}"/>

                <se:StswLabel Content="AttForceRallyPercentage:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttForceRallyPercentage}"/>

                <se:StswLabel Content="Unknown129:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Value="{Binding Unknown129}"/>

                <se:StswLabel Content="AttAssaultDelay:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttAssaultDelay}"/>

                <se:StswLabel Content="AttUnitPatrolRecommandDelay:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitPatrolRecommandDelay}"/>

                <se:StswLabel Content="Unknown132:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Value="{Binding Unknown132}"/>

                <se:StswLabel Content="SiegeEngine1:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SiegeEngine1}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SiegeEngine2:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SiegeEngine2}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SiegeEngine3:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SiegeEngine3}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SiegeEngine4:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SiegeEngine4}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SiegeEngine5:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SiegeEngine5}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SiegeEngine6:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SiegeEngine6}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SiegeEngine7:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SiegeEngine7}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="SiegeEngine8:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding SiegeEngine8}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="CowThrowInterval:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding CowThrowInterval}"/>

                <se:StswLabel Content="Unknown142:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Value="{Binding Unknown142}"/>

                <se:StswLabel Content="AttMaxEngineers:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttMaxEngineers}"/>

                <se:StswLabel Content="AttDiggingUnit:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttDiggingUnit}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttDiggingUnitMax:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttDiggingUnitMax}"/>

                <se:StswLabel Content="AttUnitVanguard:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitVanguard}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttUnitVanguardMax:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitVanguardMax}"/>

                <se:StswLabel Content="AttMaxAssassins:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttMaxAssassins}"/>

                <se:StswLabel Content="AttMaxLaddermen:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttMaxLaddermen}"/>

                <se:StswLabel Content="AttMaxTunnelers:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttMaxTunnelers}"/>

                <se:StswLabel Content="AttUnitPatrol:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitPatrol}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttUnitPatrolMax:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitPatrolMax}"/>

                <se:StswLabel Content="AttUnitPatrolGroupsCount:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitPatrolGroupsCount}"/>

                <se:StswLabel Content="AttUnitBackup:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitBackup}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttUnitBackupMax:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitBackupMax}"/>

                <se:StswLabel Content="AttUnitBackupGroupsCount:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitBackupGroupsCount}"/>

                <se:StswLabel Content="AttUnitEngage:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitEngage}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttUnitEngageMax:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitEngageMax}"/>

                <se:StswLabel Content="AttUnitSiegeDef:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitSiegeDef}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttUnitSiegeDefMax:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitSiegeDefMax}"/>

                <se:StswLabel Content="AttUnitSiegeDefGroupsCount:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttUnitSiegeDefGroupsCount}"/>

                <se:StswLabel Content="AttUnitMain1:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitMain1}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttUnitMain2:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitMain2}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttUnitMain3:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitMain3}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttUnitMain4:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:Unit}" SelectedValue="{Binding AttUnitMain4}">
                    <se:StswComboBox.ItemTemplate>
                        <DataTemplate>
                            <se:StswLabel Content="{Binding Display}" IconScale="2" IconSource="{Binding Value, Converter={local:EnumToImageSourceConverter}}" Padding="0"/>
                        </DataTemplate>
                    </se:StswComboBox.ItemTemplate>
                </se:StswComboBox>

                <se:StswLabel Content="AttMaxDefault:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttMaxDefault}"/>

                <se:StswLabel Content="AttMainGroupsCount:"/>
                <se:StswIntegerBox Margin="3" HorizontalContentAlignment="Center" Minimum="0" Value="{Binding AttMainGroupsCount}"/>

                <se:StswLabel Content="TargetChoice:"/>
                <se:StswComboBox Margin="3" ItemsSource="{se:StswEnumToList local:TargetChoice}" SelectedValue="{Binding TargetChoice}"/>

            </se:StswLabelPanel>
        </se:StswScrollView>
    </DockPanel>
</UserControl>
